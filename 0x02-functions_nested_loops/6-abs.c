#include "main.h"

/**
 * _abs - retuns the absolute value of an integer
 * @n: the number to check
 *
 * Return: absolute integer
 */
int _abs(int n)
{
	if (n > 0)
	{
	return (n);
	}
	else
	{
	return (n * -1);
	}
}
