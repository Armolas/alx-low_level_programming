#include <stdio.h>

/**
 * main - entry point
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: 0
 */
int main(int argc, __attribute__((unused)) char *argv[])
{
	int i, sum;

	for (i = 0 ; i < argc ; i++)
	{
		sum = i;
	}
	printf("%d\n", sum);
	return (0);
}
