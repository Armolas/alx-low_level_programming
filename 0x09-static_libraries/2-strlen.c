#include "main.h"

/**
 * _strlen - returns lenght of a string
 * @s: the string to return
 *
 * Return: the lenght of a string
 */
int _strlen(char *s)
{
	int a;

	for (a = 0 ; s[a] != '\0' ; a++)
	{
	}
	return (a);
}
